{% extends 'base.html.twig' %}

{% block body %}
    <h1>Art</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ art.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ art.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('art_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('art_edit', { 'id': art.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
